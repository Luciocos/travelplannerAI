name: ci

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  calidad:
    name: calidad
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: asistente_viajes_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/asistente_viajes_test
      # Los tests marcados @pytest.mark.db (conversaciones.py) usan esta
      # variable en vez de DATABASE_URL porque conftest.py borra
      # DATABASE_URL de os.environ en cada test para aislar al resto de
      # los tests del .env real de quien los corre.
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/asistente_viajes_test
      LLM_PROVIDER: gemini
      GEMINI_MODEL: gemini-3.5-flash-lite
      # Valor ficticio a proposito: ningun test toca la red ni consume cuota.
      GEMINI_API_KEY_1: clave-ficticia-para-ci

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install ruff

      - name: Habilitar la extension pgvector
        run: |
          sudo apt-get update && sudo apt-get install -y postgresql-client
          PGPASSWORD=postgres psql -h localhost -U postgres -d asistente_viajes_test \
            -c "CREATE EXTENSION IF NOT EXISTS vector;"

      - name: Aplicar el esquema
        run: python -m scripts.inicializar_db

      - name: Lint
        run: ruff check .

      - name: Formato
        run: ruff format --check .

      - name: Tests
        run: pytest -v
